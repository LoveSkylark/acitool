# ACI Tool Configuration
# Copy this file to .env and fill in your values

# APIC URL (required)
# The URL of your APIC controller
APIC_URL=https://apic.example.com

# APIC Credentials (optional)
#
# If these variables are not set, the tool will prompt for credentials
# interactively on first run.
#
# After first successful login, the tool automatically generates and caches
# an authentication token in ~/.aci_token, so you won't need to enter
# credentials again until the token expires.
#
#APIC_USERNAME=admin
#APIC_PASSWORD=your_password_here
